import MultComponent from './MultComponent'

const FirstComponent = ()=>{
    return (
        <div>
            <h1>Meu primeiro componente</h1>
            <MultComponent/>
        </div>
    );
};

export default FirstComponent;